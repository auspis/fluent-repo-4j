spring:
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

logging:
  level:
    io.github.auspis.fluentrepo4j: DEBUG
    org.springframework.data: DEBUG
